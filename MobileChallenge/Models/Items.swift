import Foundation

class Items {
    
    let name: String
    let value: Int
    let amount: Double
    let total: Double
    
    init(name: String, value: Int, amount: Double, total: Double){
        self.name = name
        self.value = value
        self.amount = amount
        self.total = total
    }
}
