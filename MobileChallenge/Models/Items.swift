import Foundation

class Items {
    
    let name: String
    let value: Double
    let amount: Int
    let total: Double
    
    init(name: String, value: Double, amount: Int, total: Double){
        self.name = name
        self.value = value
        self.amount = amount
        self.total = total
    }
}
